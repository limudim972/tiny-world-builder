# Agent Notes

- Use UTF-8 when reading or writing text files:
  - `Get-Content -Encoding UTF8`
  - `Set-Content -Encoding UTF8`
- use `apply_patch` for edits so file encoding is preserved.
- `C` by the user meens commit.
